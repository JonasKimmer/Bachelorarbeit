# ⚽ KI-gestütztes Liveticker-Redaktionssystem

Automatisierte Generierung von Liveticker-Texten mit Claude AI für Fußballspiele.

## 🎯 Überblick

Dieses System importiert Live-Fußballdaten von API-Football und generiert automatisch Ticker-Texte in verschiedenen Stilen (neutral, euphorisch, kritisch) mithilfe von Claude AI.

### Features

- ✅ Automatischer Import von Spielen, Teams und Events
- ✅ Live-Tracking von Spielereignissen (Tore, Karten, Wechsel)
- ✅ KI-gestützte Ticker-Generierung in 3 Stilen
- ✅ Echtzeit-Updates alle 5 Sekunden
- ✅ Multi-Liga-Support (Bundesliga, La Liga, etc.)
- ✅ Responsive Web-Interface

---

## 🏗️ Architektur
```
┌─────────────────┐
│  API-Football   │ (Externe Daten)
└────────┬────────┘
         │
    ┌────▼────┐
    │   n8n   │ (Workflow-Automation)
    └────┬────┘
         │
    ┌────▼────────┐
    │ PostgreSQL  │ (Datenbank)
    └────┬────────┘
         │
    ┌────▼─────────┐
    │ FastAPI      │ (Backend)
    │ + Claude AI  │
    └────┬─────────┘
         │
    ┌────▼────┐
    │  React  │ (Frontend)
    └─────────┘
```

---

## 📦 Tech Stack

### Backend
- **Python 3.12** - FastAPI Framework
- **PostgreSQL 15** - Datenbank
- **SQLAlchemy** - ORM
- **Anthropic Claude** - LLM für Textgenerierung

### Frontend
- **React 18** - UI Framework
- **Axios** - HTTP Client
- **CSS** - Styling

### Automation
- **n8n** - Workflow-Automation
- **API-Football** - Datenquelle

---

## 🚀 Installation & Setup

### Voraussetzungen
```bash
- Python 3.12+
- Node.js 18+
- PostgreSQL 15+
- Docker (für n8n)
- API-Football Account
- Anthropic API-Key
```

### 1. Repository klonen
```bash
git clone <repository-url>
cd Bachelorarbeit
```

### 2. PostgreSQL Setup
```bash
# PostgreSQL Container starten
docker run --name liveticker-db \
  -e POSTGRES_USER=liveticker_user \
  -e POSTGRES_PASSWORD=dev_password_123 \
  -e POSTGRES_DB=liveticker_dev \
  -p 5433:5432 \
  -d postgres:15

# DB-Schema wird automatisch beim Backend-Start erstellt
```

### 3. Backend Setup
```bash
cd backend

# Virtual Environment erstellen
python -m venv venv
source venv/bin/activate  # macOS/Linux
# oder: venv\Scripts\activate  # Windows

# Dependencies installieren
pip install -r requirements.txt

# .env Datei erstellen
cat > .env << EOL
DATABASE_URL=postgresql://liveticker_user:dev_password_123@localhost:5433/liveticker_dev
ANTHROPIC_API_KEY=your_api_key_here
ANTHROPIC_MODEL=claude-sonnet-4-20250514
EOL

# Backend starten
uvicorn app.main:app --reload
```

**Backend läuft auf:** `http://localhost:8000`  
**API Docs:** `http://localhost:8000/api/docs`

### 4. Frontend Setup
```bash
cd frontend

# Dependencies installieren
npm install

# Frontend starten
npm start
```

**Frontend läuft auf:** `http://localhost:3000`

### 5. n8n Setup
```bash
# n8n Container starten
docker run -it --rm \
  --name n8n \
  -p 5678:5678 \
  -v ~/.n8n:/home/node/.n8n \
  n8nio/n8n

# n8n öffnen: http://localhost:5678

# API-Football Credentials anlegen:
# - Name: "Header Auth account"
# - Header Name: x-rapidapi-key
# - Header Value: <your_api_football_key>

# PostgreSQL Credentials anlegen:
# - Host: host.docker.internal (macOS/Windows) oder localhost
# - Port: 5433
# - Database: liveticker_dev
# - User: liveticker_user
# - Password: dev_password_123
```

### 6. Workflows importieren

**Import Teams:**
1. n8n öffnen
2. Workflow importieren aus `n8n-workflows/import-teams.json`
3. API-Football Credentials zuweisen
4. Ausführen

**Import Matches:**
1. Workflow importieren aus `n8n-workflows/import-matches.json`
2. Liga & Spieltag anpassen
3. Ausführen

**Live Event Tracking:**
1. Workflow importieren aus `n8n-workflows/live-events.json`
2. Match-ID anpassen
3. Workflow aktivieren (läuft alle 10 Sek)

---

## 📖 Verwendung

### 1. Daten importieren
```bash
# 1. Teams importieren (einmalig)
n8n Workflow: "Import Teams" ausführen

# 2. Matches importieren
n8n Workflow: "Import Matches" ausführen
# Anpassen: Liga-ID (78 = Bundesliga), Spieltag

# 3. Events importieren
n8n Workflow: "Import Events" ausführen
# Anpassen: Match-ID
```

### 2. Live-Tracking starten
```bash
# n8n Workflow "Live Events" aktivieren
# - Match-ID des Live-Spiels eintragen
# - Workflow aktivieren (Toggle ON)
# - Läuft automatisch alle 10 Sekunden
```

### 3. Frontend verwenden
```
1. http://localhost:3000 öffnen
2. Liga auswählen (z.B. Bundesliga 2025)
3. Spieltag auswählen
4. Match auswählen
5. Events werden angezeigt
6. Ticker generieren:
   - Klick auf "📝 Neutral" / "🎉 Euphorisch" / "🔍 Kritisch"
   - KI generiert Ticker-Text
   - Text wird angezeigt
```

---

## 🗂️ Projektstruktur
```
Bachelorarbeit/
├── backend/
│   ├── app/
│   │   ├── api/v1/          # API Endpoints
│   │   │   ├── events.py
│   │   │   ├── matches.py
│   │   │   ├── teams.py
│   │   │   ├── ticker_entries.py
│   │   │   └── leagues.py
│   │   ├── core/            # Config & DB
│   │   │   ├── config.py
│   │   │   └── database.py
│   │   ├── models/          # SQLAlchemy Models
│   │   │   ├── team.py
│   │   │   ├── match.py
│   │   │   ├── event.py
│   │   │   └── ticker_entry.py
│   │   ├── repositories/    # Data Access Layer
│   │   ├── schemas/         # Pydantic Schemas
│   │   ├── services/        # Business Logic
│   │   │   └── llm_service.py
│   │   └── main.py
│   ├── requirements.txt
│   └── .env
├── frontend/
│   ├── src/
│   │   ├── LiveTicker.js    # Hauptkomponente
│   │   ├── LiveTicker.css
│   │   ├── App.js
│   │   └── index.js
│   ├── package.json
│   └── public/
├── n8n-workflows/
│   ├── import-teams.json
│   ├── import-matches.json
│   └── live-events.json
└── README.md
```

---

## 🔧 Konfiguration

### API-Football Liga-IDs
```
78  - Bundesliga (Deutschland)
39  - Premier League (England)
140 - La Liga (Spanien)
135 - Serie A (Italien)
61  - Ligue 1 (Frankreich)
```

### Ticker-Stile

- **Neutral:** Sachlich, objektiv
- **Euphorisch:** Begeistert, emotional
- **Kritisch:** Analytisch, hinterfragend

### LLM-Modelle
```python
# In .env
ANTHROPIC_MODEL=claude-sonnet-4-20250514  # Empfohlen
# Alternativen:
# - claude-opus-4-5-20251101 (Premium)
# - claude-haiku-4-5-20251001 (Schnell)
```

---

## 📊 Datenbank-Schema

### Teams
```sql
id, external_id, name, code, logo_url, country, founded, 
venue_name, venue_capacity, created_at, updated_at
```

### Matches
```sql
id, external_id, home_team_id, away_team_id, match_date, 
status, league_id, league_name, season, round, 
score_home, score_away, created_at, updated_at
```

### Events
```sql
id, match_id, minute, extra_time, team_id, player_id, 
player_name, assist_id, assist_name, type, detail, 
comments, created_at
```

### Ticker Entries
```sql
id, event_id, text, style, llm_model, created_at
```

---

## 🐛 Troubleshooting

### Backend startet nicht
```bash
# PostgreSQL Connection prüfen
psql -h localhost -p 5433 -U liveticker_user -d liveticker_dev

# .env Datei prüfen
cat backend/.env
```

### Events kommen nicht an
```bash
# DB prüfen
SELECT COUNT(*) FROM events WHERE match_id = X;

# n8n Executions prüfen (Workflow Fehler?)
# API-Football Credits prüfen (Limit erreicht?)
```

### Ticker werden nicht generiert
```bash
# ANTHROPIC_API_KEY in .env gesetzt?
# Backend Logs prüfen:
# "⚠️  LLM Service läuft im MOCK-Modus" → API-Key fehlt!
```

### Frontend lädt nicht
```bash
# Backend erreichbar?
curl http://localhost:8000/health

# CORS-Fehler? → Backend .env prüfen
```

---

## 📝 API-Endpunkte

### Teams
```
GET    /api/v1/teams           - Alle Teams
GET    /api/v1/teams/{id}      - Team nach ID
POST   /api/v1/teams           - Team erstellen
```

### Matches
```
GET    /api/v1/matches                    - Alle Matches
GET    /api/v1/matches?league_id=78       - Matches einer Liga
GET    /api/v1/matches/{id}               - Match nach ID
POST   /api/v1/matches                    - Match erstellen
PATCH  /api/v1/matches/{id}               - Match aktualisieren
```

### Events
```
GET    /api/v1/events                     - Alle Events
GET    /api/v1/events?match_id=5          - Events eines Matches
GET    /api/v1/events/{id}                - Event nach ID
POST   /api/v1/events                     - Event erstellen
```

### Ticker
```
GET    /api/v1/ticker/match/{match_id}    - Ticker eines Matches
POST   /api/v1/ticker/generate/{event_id}?style=neutral - Ticker generieren
```

### Leagues
```
GET    /api/v1/leagues                    - Alle Ligen
```

---

## 🔐 Sicherheit

**Produktions-Deployment:**
```bash
# .env anpassen
DATABASE_URL=postgresql://user:pass@prod-host:5432/liveticker_prod
ANTHROPIC_API_KEY=sk-ant-...
CORS_ORIGINS=https://your-domain.com

# Secrets nicht committen!
echo ".env" >> .gitignore
```

---

## 📄 Lizenz

Bachelorarbeit - Eintracht Frankfurt / EintrachtTech GmbH

---

## 👤 Autor

**Jonas Kimmer**  
Werkstudent EintrachtTech GmbH  
Wirtschaftsinformatik Bachelor, 9. Semester

---

## 🙏 Danksagung

- API-Football für Datenquelle
- Anthropic für Claude AI
- n8n für Workflow-Automation





